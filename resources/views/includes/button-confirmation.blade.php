<button class="jsButtonConfirmation btn {{ $btnClass ?? null }} {{ $btnIcon ?? null }}"
    data-type="{{ $btnType ?? null }}" data-message="{{ $btnMessage ?? null }}"
    data-action="{{ $btnAction ?? null }}">{{ $btnText ?? null }}</button>
